#PROPERTIES

# INSTALL
function Install-Tarsvin($package) {
  $id = $package.Id;
  $version = $package.Version;
}

# UNINSTALL
function Uninstall-Tarsvin($package) {
  $id = $package.Id;
  $version = $package.Version;
}